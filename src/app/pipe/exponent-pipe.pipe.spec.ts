import { ExponentPipePipe } from './exponent-pipe.pipe';

describe('ExponentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExponentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
